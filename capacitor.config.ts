import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'jajalionic-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
